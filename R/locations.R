## Look up common locations for pairs

##' @param x object returned by \code{\link{a2bcovid}}
##'
get_common_locs <- function(x){


}

## This is not available in output. Have to read files and process them
## Perhaps this is better done in C.
## What format to return.  tidy would be from,to,date
## make expanded datafame, one row for every person x location.
## then subset for two of interest. tabulate dates. keep rows where same date occurs in both.
